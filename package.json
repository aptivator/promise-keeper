{
  "name": "promise-keeper",
  "version": "0.0.1",
  "description": "Minimalist (and full) implementation of JavaScript Promises",
  "scripts": {
    "build": "rollup -c",
    "test": "mocha ./test/*.js",
    "watch": "npm test && run-when-changed --watch \"dist/promise-keeper\" --watch \"test/*.js\" --exec \"npm test\"",
    "dev": "rollup -c -w & npm run watch"
  },
  "main": "dist/promise-keeper.js",
  "jsnext:main": "dist/promise-keeper.es.js",
  "author": "Dmitriy A. Nesterkin",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/aptivator/promise-keeper.git"
  },
  "bugs": {
    "url": "https://github.com/aptivator/promise-keeper/issues"
  },
  "devDependencies": {
    "buble": "^0.15.2",
    "chai": "^3.5.0",
    "mocha": "^3.2.0",
    "rollup": "^0.41.6",
    "rollup-plugin-buble": "^0.15.0",
    "rollup-watch": "^3.2.2",
    "run-when-changed": "^2.0.3"
  }
}
